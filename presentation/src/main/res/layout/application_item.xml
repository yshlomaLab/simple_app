<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:application="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
              android:orientation="horizontal"
              android:layout_width="match_parent"
              android:padding="@dimen/material_margin"
              android:layout_height="@dimen/application_item_height">

    <ImageView
            android:id="@+id/icon"
            android:layout_width="@dimen/application_icon_size"
            android:layout_height="@dimen/application_icon_size"
            application:srcCompat="@mipmap/ic_launcher_round"/>

    <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:paddingLeft="@dimen/material_margin"
            android:layout_weight="1"
            android:textSize="@dimen/text_size"
            android:gravity="center_vertical"
            tools:text="Package Name"/>

    <TextView
            android:id="@+id/rating"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:textSize="@dimen/text_size"
            android:gravity="center_vertical"
            tools:text="Rating: 4.1"/>

</LinearLayout>